package com.wisehr.wisehr.schedule.repository;

import com.wisehr.wisehr.schedule.entity.ScheduleSearchWorkPattern;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScheduleSearchWorkPatternRepository extends JpaRepository<ScheduleSearchWorkPattern, Integer> {
}
